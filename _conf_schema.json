{
  "rag": {
    "type": "object",
    "description": "[RAG] 文档知识库与动态记忆",
    "hint": "上传大段文档进行语义检索，或自动压缩并提取长期对话记忆。",
    "items": {
      "llm_provider_id": {
        "type": "string",
        "description": "摘要 LLM 提供商",
        "_special": "select_provider",
        "default": "",
        "hint": "用于将聊天对话自动压缩为极简记忆摘要。留空则使用简单的文本截断。"
      },
      "embedding_provider_id": {
        "type": "string",
        "description": "Embedding 提供商",
        "_special": "select_provider_embedding",
        "default": "",
        "hint": "用于文档与记忆的文本向量化。留空使用本地模型 BAAI/bge-small-zh-v1.5。注意：切换提供商会导致向量维度变化，已上传的 RAG 文档需重新上传。"
      },
      "rerank_provider_id": {
        "type": "string",
        "description": "重排(Rerank) 提供商",
        "_special": "select_provider_rerank",
        "default": "",
        "hint": "可选。对向量初筛的候选进行二次打分，极大提升检索精准度。"
      },
      "enable_local_embedding": {
        "type": "bool",
        "description": "启用本地嵌入模型",
        "default": true,
        "hint": "开启后，若 API 提供商失效或未配置，将自动回退到本地模型运行。"
      },
      "chunk_size": {
        "type": "int",
        "description": "文档分块大小 (字符)",
        "default": 500,
        "hint": "每个文本块的字符数。建议 300-800。"
      },
      "chunk_overlap": {
        "type": "int",
        "description": "分块重叠 (字符)",
        "default": 50,
        "hint": "相邻块的重叠字数，保证语义连贯。建议 30-100。"
      },
      "top_k": {
        "type": "int",
        "description": "检索最终注入数量",
        "default": 3,
        "hint": "每次最终注入到系统提示词的片段数。建议 2-4。"
      },
      "dense_top_k": {
        "type": "int",
        "description": "FAISS 粗排数量",
        "default": 5,
        "hint": "向量库初步召回的数量，随后由重排模型精筛。建议为最终注入量的 2-3 倍。"
      },
      "enable_memory": {
        "type": "bool",
        "description": "启用动态记忆",
        "default": false,
        "hint": "开启后，在后台自动进行内容压缩与特征提取，跨会话保留长期记忆（不会阻塞聊天）。"
      },
      "enable_autonomous_reflection": {
        "type": "bool",
        "description": "启用全自动闲时反思",
        "default": true,
        "hint": "开启后，系统在闲置时自动抽取并提纯过去累积的对话记录，生成高级维度的心理特征和客观事实。"
      },
      "enable_chat_logging": {
        "type": "bool",
        "description": "启用本地对话归档",
        "default": true,
        "hint": "开启后，按当前角色和会话完整保存所有原始对话历史。可用于导出剧本或未来辅助记忆。"
      },
      "chat_log_retention_days": {
        "type": "int",
        "description": "对话日志保留天数",
        "default": 30,
        "hint": "超过保留天数的对话日志将被自动清理。设为 0 表示永久保留。"
      }
    }
  },
  "worldbook": {
    "type": "object",
    "description": "[世界书] 设定按需注入",
    "hint": "通过关键词触发，将设定按需注入 LLM，避免一次性塞入全部设定导致大模型注意力稀释。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用世界书",
        "default": true,
        "hint": "世界书总开关。关闭后所有会话都不再注入世界书（常驻条目与关键词条目一并停用）；角色卡上的单卡绑定设置仍然保留，重新开启即恢复。"
      },
      "max_dynamic_entries": {
        "type": "int",
        "description": "触发条目上限",
        "default": 4,
        "hint": "防止一次命中过多关键词导致提示词爆炸。建议 2-6。"
      },
      "max_token_limit": {
        "type": "int",
        "description": "注入 Token 估算上限",
        "default": 4000,
        "hint": "[重要] 建议控制在 3000-4000 左右，太高会导致大模型看了前面忘后面，丢失输出格式的能力。"
      },
      "match_sensitivity": {
        "type": "float",
        "description": "模糊匹配灵敏度",
        "default": 0.7,
        "hint": "0.0 = 极其严格，1.0 = 极其宽松。默认 0.7 平衡。",
        "slider": {"min": 0, "max": 1, "step": 0.1}
      },
      "injection_position": {
        "type": "string",
        "description": "提示词注入位置",
        "default": "user_prefix",
        "hint": "[强烈推荐 user_prefix] 将强制协议贴脸输出在用户消息之前，能极大提升大模型对状态栏和工具调用的服从度！",
        "options": ["system_end", "user_prefix"]
      },
      "show_trigger_log": {
        "type": "bool",
        "description": "显示触发日志",
        "default": false,
        "hint": "调试用。开启后，Web 面板的插件状态里会列出最新一轮世界书命中了哪些条目（含命中的关键词）；也可用 /quill debug 查看注入构成。"
      },
      "always_activate": {
        "type": "bool",
        "description": "全局常驻激活",
        "default": false,
        "hint": "开启后，每次对话都会强制注入系统提示词和状态栏，无需触发激活词。适合纯RP场景。"
      }
    }
  },
  "writing_resource": {
    "type": "object",
    "description": "[写作素材库] 风格指引注入",
    "hint": "管理写作风格、动作控制。通过关键词匹配按需注入。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用素材库",
        "default": true,
        "hint": "关闭后停用素材库系统。"
      },
      "max_entries": {
        "type": "int",
        "description": "最大注入条数",
        "default": 4,
        "hint": "单次注入素材上限。建议 3-5 条，避免稀释大模型注意力。"
      },
      "fallback_top_count": {
        "type": "int",
        "description": "无匹配时回退条数",
        "default": 2,
        "hint": "未命中时注入历史命中率最高的风格作为兜底。"
      },
      "category_dedup_limit": {
        "type": "int",
        "description": "同类去重上限",
        "default": 3,
        "hint": "同分类素材最多注入条数，避免某一类内容过多。"
      }
    }
  },
  "performance": {
    "type": "object",
    "description": "[表现控制] 字数与截断",
    "hint": "控制 Prompt 的强硬限制与底层安全门槛。",
    "items": {
      "max_prompt_length": {
        "type": "int",
        "description": "提示词硬截断上限 (字符)",
        "default": 50000,
        "hint": "总字符数极限保护，超出按优先级强行丢弃次要内容。"
      },
      "min_output_length": {
        "type": "int",
        "description": "要求回复最低字数",
        "default": 400,
        "hint": "[重要] 要求 LLM 写长文。建议 400-800。字数要求过低会导致大模型进入短聊模式忽略一切格式。"
      },
      "max_output_length": {
        "type": "int",
        "description": "限制回复最高字数",
        "default": 0,
        "hint": "限制 LLM 回复的最大字符数。0 表示无限制。建议 1000-3000 避免回复过长。"
      }
    }
  },
  "status_bar": {
    "type": "object",
    "description": "[状态栏] 沉浸式属性追踪",
    "hint": "底层自动截获 LLM 输出并渲染为精美 UI 面板。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用状态栏",
        "default": true,
        "hint": "[核心开关] 开启后，大模型将在工具调用时自动打包输出状态栏，插件负责解析并渲染；关闭则仅输出纯剧情，并自动清除历史里残留的状态栏痕迹。这是**全局默认值**——单个会话可用聊天指令 /quill statusbar on|off 临时覆盖（覆盖值不写回本配置）。"
      },
      "fields": {
        "type": "text",
        "description": "状态栏字段（管道分隔）",
        "default": "好感度|关系阶段|心情|位置|穿着|当前想法",
        "hint": "用 | 分隔字段名，可自由替换词汇（如 服从度|发情度）。建议填满 6 个：不足 6 个会自动补空位，状态栏会出现空字段行。改这里会同步影响提示词契约、剥离器与解析器的字段白名单，无需重启即生效。"
      },
      "format_template": {
        "type": "text",
        "description": "UI 渲染模板",
        "default": "**状态栏**\n```\n{content}\n```",
        "hint": "控制状态栏在聊天窗口中的最终 Markdown 渲染样式。"
      },
      "plot_paths": {
        "type": "text",
        "description": "剧情走向选项",
        "default": "继续当前话题|转换场景|结束互动",
        "hint": "状态栏下方显示的分支选项，用 | 分隔。留空则使用内置默认三项（继续当前话题|转换场景|结束互动）——**不是**不显示。"
      },
      "llm_extract": {
        "type": "bool",
        "description": "LLM 智能提取（实验性）",
        "default": false,
        "hint": "当六级降级链（L1 代码块 → L2 [LOVE_DATA] → L3 [STATUS] → L4 裸字段 → L5 宽松解析）全部失败时，调用轻量 LLM 从文本中提取状态字段。3s 超时保护，失败自动放弃。默认关闭（会额外消耗 token）。"
      },
      "llm_provider_id": {
        "type": "string",
        "description": "状态栏提取 LLM 提供商",
        "_special": "select_provider",
        "default": "",
        "hint": "状态栏智能提取所使用的 LLM。留空则回退到 RAG 摘要 LLM。建议配置轻量/便宜的模型以降低成本。"
      },
      "default_placeholder": {
        "type": "string",
        "description": "缺省占位符文本",
        "default": "未设置",
        "hint": "当状态栏字段未匹配到值时显示的占位符。可自定义，如「未知」「—」等。"
      },
      "show_delta": {
        "type": "bool",
        "description": "标注字段变化幅度",
        "default": true,
        "hint": "开启后，可量化的状态字段（如好感度）相对上一轮的变化会带上标注，形如「好感度：84/100（↑2）」。文本字段（心情、位置、当前想法等）不加标注——它们每轮都在变，新值本身就说明了变化，挂箭头只会淹没真正有价值的数值变化。"
      },
      "format_template_plain": {
        "type": "text",
        "description": "纯文本平台渲染模板",
        "default": "───── 状态栏 ─────\n{content}\n────────────────",
        "hint": "QQ/微信这类不渲染 Markdown 的平台用这份模板（{content} 为内容占位符）。默认模板里的 `**状态栏**` 与 ``` 围栏在纯文本平台会原样显示，故改用分隔线排版。留空则退回上面那份模板。"
      },
      "plain_platforms": {
        "type": "text",
        "description": "按纯文本渲染的平台（管道分隔）",
        "default": "",
        "hint": "填 AstrBot 的适配器名，多个用 | 分隔，如 aiocqhttp|qq_official。留空使用内置默认：aiocqhttp、qq_official、qq_official_webhook、wecom、wecom_ai_bot、weixin_oc、weixin_official_account、dingtalk、line。未知平台一律走 Markdown 模板（不破坏原生渲染）。"
      }
    }
  },
  "refusal": {
    "type": "object",
    "description": "[反拒绝] 兜底应急协议",
    "hint": "一旦检测到大模型道德审查拒答，下次强制注入最高权重越狱指令。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用拒绝检测",
        "default": true,
        "hint": "关闭后不再检测拒绝行为。"
      },
      "patterns": {
        "type": "text",
        "description": "拒绝匹配模式（每行一条）",
        "default": "我不能\n我无法\n这违反\n我不应该\n这不合适\n我拒绝",
        "hint": "大模型回复中包含任一模式即判定为触发了安全审查。"
      }
    }
  },
  "debug": {
    "type": "object",
    "description": "[调试]",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用调试日志",
        "default": false,
        "hint": "开启后在控制台打印极其详细的匹配详情（会刷屏，非开发排错勿开）。"
      },
      "show_inject_report": {
        "type": "bool",
        "description": "在回复末尾显示注入报告",
        "default": false,
        "hint": "开启后每轮回复末尾附一行「〔注入〕世界书×2 · 记忆×3 · 素材×2 · 文档×1（来源名）」，用于核对灵敏度/top_k 等参数的调整效果。会进入聊天记录，纯沉浸 RP 时可关闭；关闭后仍可用 /quill debug 查看上一轮构成。"
      }
    }
  },
  "permissions": {
    "type": "object",
    "description": "[权限] 管理员白名单",
    "hint": "控制群聊写指令的访问权限。仅作用于 QQ/Discord 等聊天平台的群聊写指令，Web 面板已由 AstrBot 鉴权保护，不受此项影响。",
    "items": {
      "admin_users": {
        "type": "text",
        "description": "管理员 ID（换行分隔）",
        "default": "",
        "hint": "填写 AstrBot 账号 ID（UID，可通过 /sid 指令获取），每行一个。配置后仅白名单用户可在群聊执行写指令（切卡/清记忆/改流式等）。留空时群聊写指令被拦截，私聊与 Web 面板编辑不受限制。"
      }
    }
  }
}
